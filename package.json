{
  "name": "ha-solar-rate-switching-automation",
  "version": "1.0.0",
  "description": "CLI tool for determining the optimal time to switch electricity rate plans using Home Assistant energy data",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  },
  "dependencies": {
    "dotenv": "^17.4.0"
  }
}
